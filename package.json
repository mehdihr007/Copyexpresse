{
  "name": "bouda-copy-express",
  "version": "1.0.0",
  "scripts": {
    "start": "npx @11ty/eleventy --serve",
    "build": "node -e \"require('fs').rmSync('_site',{recursive:true,force:true})\" && npx @11ty/eleventy"
  },
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1"
  }
}
